[project]
name = "mcp-xray"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raksab01", email = "rakeshsabbisetty2001@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.3.0",
    "mcp>=2.1.1",
    "pydantic>=2.13.5",
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
]

[project.scripts]
mcp-xray = "mcp_xray.cli:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
